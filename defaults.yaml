primary_device: 'cuda'
seed: 69
exp_details:
  data_root: C:\Users\axhol\Documents\Personal Projects\CS560-project\data
  out_dir: C:\Users\axhol\Documents\Personal Projects\Python\DSA-starter\data # Model + figure path
  exp_id:          # Optional suffix for autogenerated experiment names
  dataset: ADNI
train_params:
  epochs: 200
  checkpoint: 100
  batch_size: 128
  loss_fn: 'DeepHit'
  reduction: 'mean'   # [sum, mean] --> Default: mean
  lambda: 1.0           # Ranking loss weight
model:
  backbone: resnet18  # [resnet18, resnet50, vit]
  in_dim: 9            # For nonimaging data 
  mlp_hid_dim: 28     # Mlp only
  pretrained_path: 
optimizer:
  optim: 'Adam'     # [SGD, Adam] --> Default: Adam
  lr: .0001  
  weight_decay: 0   # Usually 0
  momentum: 0       # Usually 0
  lr_scheduler: 
